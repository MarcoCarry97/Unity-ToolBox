using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GuardMove : StatusMove
{
    // Start is called before the first frame update
    void Start()
    {
        Name = "Guard";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
